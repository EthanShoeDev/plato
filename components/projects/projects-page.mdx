import { Button } from '../ui/button';
import { Card } from '../ui/card';

export const foo = {
  hi: `Fudge ${Button.displayName || 'Button'}`,
  authors: ['fred', 'sally'],
};

export const hello = 'hello';

# Hello, world!

# Hello, world!

I'm an awesome paragraph.

{/* I'm a comment */}

<Card bg="red">
    <Button>hi                                            </Button>

{ hello }

{/* another comment */}

</Card>

```plaintext
test codeblock
```

```js
module.exports = 'test';
```

```sh
npm i -g Card
```

| Test | Table |
| :--- | :---- |
| Col1 | Col2  |

export   default     ({children   }) => < div>{    children}</div

>
